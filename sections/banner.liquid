<!-- filepath: sections/banner.liquid -->
{% schema %}
{
  "name": "Banner",
  "settings": [
    {
      "type": "image_picker",
      "id": "banner_image",
      "label": "Banner Image"
    },
    {
      "type": "text",
      "id": "banner_heading",
      "label": "Banner Heading",
      "default": "The Gift Guide"
    },
    {
      "type": "text",
      "id": "banner_subheading",
      "label": "Banner Subheading",
      "default": "Your Banner Subheading"
    },
    {
      "type": "url",
      "id": "banner_button_link",
      "label": "Button Link"
    },
    {
      "type": "text",
      "id": "banner_button_text",
      "label": "Button Text",
      "default": "Shop Now"
    }
  ]
}
{% endschema %}

<div class="banner">
<div class="top-bar">
   <h1>Tisso Vision</h1>
   <h3> Find the ideal gift for your loved ones</h3>
    <a href="{{ section.settings.banner_button_link }}" class="banner-button">{{ section.settings.banner_button_text }}</a>
</div>

<img id="frame2-img" src="{{ 'Frame2.svg' | asset_url }}" alt="Banner Image"  width="600" height="400">
  <div class="banner-container">
    <div class="banner-content">
        <h1>{{ section.settings.banner_heading }}</h1>
        <p>{{ section.settings.banner_subheading }}</p>
        <a href="{{ section.settings.banner_button_link }}" class="banner-button">{{ section.settings.banner_button_text }}</a>
    </div>
    <img id="frame1-img" src="{{ 'Frame1.png' | asset_url }}" alt="Banner Image" width="600" height="400">
   </div>
</div>

<style>
  .top-bar {
    height: 170.67px;
    background: #F5F5F5;
    display: flex;
    flex-direction: row;
    justify-content: center;
    align-items: center;
    padding: 26.6667px 133.333px;
    gap: 720px;
    }
    .top-bar h1 {
        width: 403.85px;
        height: 50.67px;
    }
    .top-bar h3 {
        font-family: 'Jost';
        font-style: normal;
        font-weight: 400;
        font-size: 42.6667px;
        line-height: 36px;
        /* identical to box height, or 84% */
        display: flex;
        align-items: center;
        text-align: center;
    }

    .top-bar.banner-button {
        background: #FFF544;
        border-radius: 4px;
        font-family: 'Jost';
        font-style: normal;
        font-weight: 400;
        font-size: 42.6667px;
        line-height: 55px;
        /* identical to box height, or 128% */
        display: flex;
        align-items: center;
        text-align: center;
        text-transform: uppercase;

        color: #000000;
    }
  .banner {
    position: relative;
    text-align: center;
    color: white;
  }
  #frame2-img {
    width: 100%;
    height: auto;
  }
  .banner-content {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
  }
  .banner-content p {
    color: black;
    text-align: left;
  }
  .banner-button {
    background-color: #000;
    color: #fff;
    padding: 10px 20px;
    text-decoration: none;position: relative;
  }
  .banner-button::after {
    content: '→';
    margin-left: 10px;
    font-size: 16px;
  }
  .banner-button:hover {
    background-color: #333;
  }
  .banner-container {
    display: flex;
    justify-content: space-between;
    padding-left: 5rem;
  }
  #frame1-img {
    width: 60%
  }
</style>